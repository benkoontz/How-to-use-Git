// git commands
git init // initialize local git repository
git add <file> // add file(s) to index
git rm <file>// remove a file
git status // check status of working tree
git commit // commit changes in index
git touch <file> // add a file
git push // push to remote repository such as github
git pull // pull latest version from remote repository
git clone // clone repository into a new directory
git branch <branch name> // create new branch
git checkout <branch name> // move to that branch
git merge // used to merge files 
git push --force "https://github.com/benkoontz/practice" master


//windows git download link
http://git-scm.com/download/win

//Mac download line
http://git-scm.com/download/mac

// Linux (Debian) download
$sudo apt-get install git

// Linux (Fedora) download
$sudo yum install git


Steps:

1. do git bash in a folder location on your computer
2. do git clone  https://github.com/benkoontz/How-to-use-Git
3. you can add, remove, or make changes to files
4. commit changes using commit -m "your message"
5. make sure your changes are what you want them to be
6. push to the repository using push "https://github.com/benkoontz/How-to-use-Git"

